version: '3'

services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DEVUSER: devuser
    user: devuser
    command: "sleep infinity"
    volumes:
      - ..:/workspaces/lidarshooter